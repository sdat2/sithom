v0.0.3
 - Removed `typeguard` as a requirement.
 - Improved colormaps.
v0.0.2
 - Added `places.py` with `BoundingBox` and `Point` objects.
 - Added `typeguard` as a requirement.
 - Added `sithom.plot.lim` with tests to allow percentile limits.
